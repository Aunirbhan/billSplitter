name = "billsplitter-proxy"
main = "worker.js"
compatibility_date = "2026-08-01"

# Runs on the owner's domain instead of workers.dev
workers_dev = false
routes = [
  { pattern = "api.aunirbhan.com", custom_domain = true }
]

# Secret (never in this file): npx wrangler secret put ANTHROPIC_API_KEY
